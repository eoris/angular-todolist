.table-responsive
  %table.table.table-hover
    %tbody(ui-sortable ng-model="project.tasks")
      %tr(ng-repeat="task in project.tasks")
        %td
          %label
            %input(type="checkbox")
        %td {{task.title}}
        %td {{task.deadline}}
